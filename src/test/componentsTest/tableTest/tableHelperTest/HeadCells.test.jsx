import { headCells } from '../../../../components/table/tableHelper/HeadCells';

describe('Table Header Options', () => {
  test('Table Header Test Cases', () => {
    expect(headCells).toBeTruthy();
  })
})