import { ChartOptions } from '../../../../components/charts/chartHelpers/ChartOptions';

describe('Chart Options', () => {
  test('Chart Options Test Cases', () => {
    expect(ChartOptions).toBeTruthy();
  })
})